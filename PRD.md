# ForceTrack 看板 Web 应用 MVP 产品需求文档

> 版本：v1.0  
> 目标交付周期：1 个工作日（约 8 小时）  
> 产品形态：桌面端优先的单用户 Web 应用  
> 参考方向：Jira 看板的信息架构与操作习惯，但仅保留一天内可完整交付的核心闭环

## 1. 文档目的

本文用于明确 ForceTrack 一日 MVP 的产品范围、交互规则、优先级与验收标准，保证在有限工时内交付一个可演示、可实际使用、可继续演进的类 Jira 看板，而不是一个只有静态界面的原型。

## 2. 产品概述

### 2.1 产品定位

ForceTrack 是一个轻量级任务管理 Web 应用，帮助个人或小团队以看板方式创建、组织和跟踪工作，并通过时间线查看任务的起止安排。

MVP 不实现真实账号、权限和实时协作。用户打开应用即可使用，数据保存在当前浏览器中。

### 2.2 核心价值

- 在一个页面内快速了解任务所处阶段。
- 通过拖拽更新任务状态，形成从创建到完成的最短操作闭环。
- 通过时间线发现任务日期冲突、空档和逾期风险。
- 支持中英文和明暗主题，满足基本的个性化与演示需求。

### 2.3 MVP 成功标准

MVP 完成时，首次访问的用户应能在 3 分钟内独立完成以下流程：

1. 创建一个任务并填写标题、优先级、负责人和日期。
2. 在看板中将任务从“待办”拖到“进行中”，刷新页面后状态仍然保留。
3. 搜索或筛选目标任务。
4. 在时间线中看到该任务的起止范围。
5. 切换语言和主题，刷新后偏好仍然保留。

## 3. 用户与使用场景

### 3.1 目标用户

- 需要轻量任务跟踪的个人开发者、设计师或项目负责人。
- 希望快速演示项目管理工作流、但不需要企业级 Jira 配置的用户。

### 3.2 核心场景

| 场景 | 用户目标 | MVP 对应能力 |
| --- | --- | --- |
| 每日任务管理 | 查看今天正在做什么、下一步做什么 | 看板、状态列、任务卡片 |
| 工作状态更新 | 快速推进任务，不进入复杂表单 | 卡片拖拽、状态同步 |
| 计划排期 | 查看任务在日期维度上的分布 | Timeline 时间线 |
| 快速定位 | 从较多任务中找到目标项 | 搜索、负责人/优先级筛选 |
| 轻量个性化 | 在不同语言、环境光线下舒适使用 | 中英文切换、浅色/深色主题 |

## 4. 产品原则

1. **闭环优先**：优先保证任务从创建、查看、编辑、流转到完成的完整链路。
2. **简单默认值**：不要求用户先配置项目、工作流或成员即可开始使用。
3. **渐进呈现**：卡片只展示关键信息，完整字段在任务抽屉中编辑。
4. **状态一致**：Board 与 Timeline 共享同一份任务数据，任何修改立即同步。
5. **可替换边界**：本地存储、身份和数据访问层保持独立，后续可以替换为服务端实现。

## 5. 信息架构

### 5.1 全局结构

```text
ForceTrack
├── 顶部栏
│   ├── 产品名称 / 当前项目
│   ├── Board / 看板
│   ├── Timeline / 时间线
│   └── 语言与主题设置
├── Board
│   ├── 搜索与筛选工具栏
│   ├── To Do / 待办
│   ├── In Progress / 进行中
│   ├── In Review / 审核中
│   └── Done / 已完成
├── Timeline
│   ├── 日期范围与今日定位
│   └── 任务列表及日期条
└── 任务详情抽屉 / 弹窗
```

### 5.2 默认工作流

MVP 固定四个状态，不提供增删列或重命名能力：

| 状态值 | 中文 | English | 含义 |
| --- | --- | --- | --- |
| `todo` | 待办 | To Do | 尚未开始 |
| `in_progress` | 进行中 | In Progress | 正在处理 |
| `in_review` | 审核中 | In Review | 等待检查或确认 |
| `done` | 已完成 | Done | 工作已结束 |

## 6. MVP 功能范围

### 6.1 P0：必须完成

#### 6.1.1 应用框架与导航

- 顶部展示产品名、当前项目名（默认“我的项目 / My Project”）和主导航。
- 主导航仅包含 Board 与 Timeline。
- 当前页面有清晰的选中态。
- 语言与主题入口固定在顶部区域，切换后无需刷新页面。

#### 6.1.2 Board 看板

- 固定展示四个状态列：待办、进行中、审核中、已完成。
- 每列标题旁展示当前任务数量。
- 任务卡片展示：
  - 标题；
  - 任务编号；
  - 优先级；
  - 截止日期（如有）；
  - 负责人头像或姓名首字母。
- 点击“新建任务”打开任务编辑抽屉或弹窗。
- 点击卡片可查看并编辑任务详情。
- 支持在四列之间拖拽卡片；放下后立即更新状态并持久化。
- 列内顺序允许通过拖拽调整并持久化；若工时受阻，至少保证跨列拖拽稳定，列内排序可按最近更新兜底。
- 空列显示明确的空状态和新建入口。

#### 6.1.3 任务创建、编辑与删除

任务字段如下：

| 字段 | 必填 | 规则 | 卡片展示 |
| --- | --- | --- | --- |
| ID | 自动 | 递增编号，如 `FT-1`，不可编辑 | 是 |
| 标题 | 是 | 1–100 个字符 | 是 |
| 描述 | 否 | 纯文本，最多 2,000 个字符 | 否 |
| 状态 | 是 | 四种固定状态，默认“待办” | 通过所在列表达 |
| 优先级 | 是 | 低、中、高；默认“中” | 是 |
| 负责人 | 否 | 从内置模拟成员中选择 | 是 |
| 开始日期 | 否 | 日期格式由界面本地化 | 否 |
| 截止日期 | 否 | 不得早于开始日期 | 是 |
| 创建时间 | 自动 | 创建时生成，不可编辑 | 否 |
| 更新时间 | 自动 | 每次修改时更新 | 否 |

交互要求：

- 标题为空时禁止保存，并显示就地错误提示。
- 截止日期早于开始日期时禁止保存并提示原因。
- 关闭未保存的编辑界面时，若内容发生变化，需要二次确认。
- 删除任务需要二次确认；确认后从 Board 与 Timeline 同时移除。
- 创建或更新成功后无需额外确认弹窗，界面应立即反映结果。

内置模拟成员建议为 3 人，使用固定 ID 和本地头像/首字母，避免为 MVP 引入成员管理功能。

#### 6.1.4 搜索与筛选

- 支持按任务标题和任务编号进行不区分大小写的关键词搜索。
- 支持按优先级筛选。
- 支持按负责人筛选，包含“未分配”。
- 搜索与多个筛选条件采用交集关系。
- 提供“一键清除”并显示当前结果数量或无结果状态。
- 搜索和筛选只影响当前展示，不修改原始任务数据。

#### 6.1.5 Timeline 时间线

MVP 采用简化时间线，不实现完整甘特图编辑器：

- 左侧按任务展示标题与状态，右侧以水平日期条展示开始至截止日期。
- 默认展示以今天为中心的连续日期窗口，并突出今天。
- 提供“今天”按钮，使时间线回到当前日期。
- 没有日期的任务集中显示在“未排期”区域，不在时间轴上绘制日期条。
- 只有一个日期时，开始日期或截止日期缺失的一端按同一天处理。
- 点击任务行或日期条打开同一个任务详情界面。
- 在任务详情中修改日期后，时间线立即更新。
- 已逾期且未完成的任务使用警示色表达；仅依赖颜色时须同时显示图标或文字。
- MVP 不支持拖拽日期条、调整长度、依赖关系、里程碑和缩放级别。

#### 6.1.6 中英文切换

- 支持简体中文 `zh-CN` 与英文 `en-US`。
- 所有用户可见的固定文本均通过统一的翻译资源维护，不在组件内散落硬编码。
- 切换语言后即时更新以下内容：导航、按钮、列名、字段名、校验、空状态和确认提示。
- 用户输入的任务标题、描述和项目名保持原文，不自动翻译。
- 日期、数字等格式跟随当前语言环境。
- 语言选择保存到本地，重新打开应用后保持。
- 首次访问默认使用浏览器语言；无法识别时默认简体中文。

#### 6.1.7 主题切换

- 右上角设置面板提供 Vercel Light、Vercel Dark 主题卡片及效果预览，并为后续主题扩展保留卡片网格。
- 主题切换即时生效并保存到本地。
- 首次访问默认 Vercel Dark 主题。
- 两种主题下均需保证文字、边框、焦点态和优先级标识清晰可辨。
- 颜色应由语义化设计令牌统一管理，避免组件写死颜色值，以便后续扩展品牌主题。

#### 6.1.8 本地数据与初始化

- 使用浏览器本地持久化保存任务、任务顺序、语言和主题偏好。
- 首次进入提供 5–8 条演示任务，覆盖不同状态、优先级、负责人和日期情况。
- 演示数据只在首次初始化时创建，不能在刷新页面时覆盖用户修改。
- 刷新或重新打开浏览器后，用户数据和偏好保持不变。
- 提供“重置演示数据”入口不是 P0；开发调试可通过清理浏览器存储完成。

### 6.2 P1：有剩余工时再做

P1 不得挤占 P0 的功能完整度与验证时间，按以下顺序实现：

1. Backlog 列表视图：展示所有未完成任务，支持创建和打开详情。
2. 时间线按周切换或水平滚动。
3. 看板按负责人分组。
4. 操作成功提示与撤销最近一次状态移动。
5. 项目名称的本地编辑。

### 6.3 明确不在本次范围

- 真实注册、登录、密码找回和第三方 OAuth。
- 多用户实时协作、评论、@提及、在线状态和冲突合并。
- 服务端数据库、云同步、跨设备同步和离线同步队列。
- 组织、空间、多项目、成员邀请、角色与权限。
- 自定义工作流、增删状态列、WIP 限制和自动化规则。
- Sprint、估时、工时填报、燃尽图、报表和仪表盘。
- 附件上传、子任务、标签管理、依赖关系和通知。
- Docs、Forms、Development 集成、第三方应用集成。
- 移动端原生应用；小屏只需保证内容可访问，不要求移动端专属交互。

## 7. 关键用户流程

### 7.1 创建并推进任务

1. 用户进入 Board。
2. 点击全局或“待办”列中的“新建任务”。
3. 填写标题及可选字段并保存。
4. 新任务出现在“待办”列，列计数增加。
5. 用户将卡片拖到“进行中”。
6. 状态立即更新；刷新页面后任务仍处于“进行中”。

### 7.2 编辑任务并查看排期

1. 用户点击一张任务卡片。
2. 在详情中设置开始日期和截止日期并保存。
3. 用户切换到 Timeline。
4. 对应任务以日期条展示在正确日期范围。
5. 用户再次点击任务行，可打开并继续编辑同一任务。

### 7.3 切换语言与主题

1. 用户从顶部入口选择 English。
2. 所有固定界面文本即时变为英文，任务内容保持原文。
3. 用户选择深色主题。
4. 页面即时切换且无明显闪烁；刷新后两项选择均保留。

## 8. 界面与交互要求

### 8.1 视觉方向

- 参考 Jira 的清晰分栏、顶部导航、工具栏和卡片层级，但不复制其品牌资产。
- 整体保持专业、克制、信息密度适中。
- 状态列在桌面宽度下横向排列；窗口不足时允许看板区域横向滚动。
- 卡片悬停、拖拽中、可放置目标和键盘焦点均应有明确反馈。

### 8.2 响应式范围

- 主要验收视口：桌面端 1280×720 及以上。
- 768–1279 px：允许顶部工具栏换行，看板保持横向滚动。
- 小于 768 px：保证导航、任务查看和编辑可用；不将移动端拖拽体验列为 MVP 验收项。

### 8.3 基础可访问性

- 所有按钮、表单控件具有可识别的文本或无障碍标签。
- 键盘可完成导航、创建、编辑、保存和取消；拖拽状态变更可通过详情中的状态下拉框替代。
- 焦点样式清晰可见。
- 不只依赖颜色表达优先级、逾期或错误。
- 文本与背景对比度以 WCAG AA 为目标。

### 8.4 异常与边界状态

- 首次加载：展示演示数据，不出现空白页面。
- 搜索无结果：展示原因说明和清除条件入口。
- Timeline 无已排期任务：展示引导用户添加日期的空状态。
- 本地数据解析失败：忽略损坏数据并恢复演示数据，同时给出非阻塞提示。
- 存储写入失败：保留当前界面状态并明确提示数据可能无法在刷新后保存。

## 9. 概念数据模型

PRD 不限定具体前端框架，但实现应围绕以下稳定实体建模：

```ts
type TaskStatus = "todo" | "in_progress" | "in_review" | "done";
type TaskPriority = "low" | "medium" | "high";

interface Task {
  id: string;
  key: string;
  title: string;
  description: string;
  status: TaskStatus;
  priority: TaskPriority;
  assigneeId: string | null;
  startDate: string | null; // YYYY-MM-DD
  dueDate: string | null;   // YYYY-MM-DD
  position: number;
  createdAt: string;
  updatedAt: string;
}

interface Member {
  id: string;
  name: string;
  avatar?: string;
}

interface UserPreferences {
  locale: "zh-CN" | "en-US";
  theme: "light" | "dark";
}
```

实现时应通过统一的数据访问接口执行任务的读取和写入，而不是让页面组件直接操作浏览器存储。后续接入 API 时，可以替换存储实现而不改动核心页面交互。

## 10. 非功能要求

### 10.1 性能

- 以 100 条本地任务为基准，Board 搜索、筛选和拖拽应无明显卡顿。
- 常见桌面网络环境下首次可交互时间目标小于 2 秒；本地开发构建不作为该指标依据。
- 页面切换不应导致完整刷新。

### 10.2 兼容性

- P0 验收浏览器：最新版 Chrome。
- 应尽量兼容最新版 Edge 和 Safari，但一天 MVP 不承诺完整跨浏览器回归。

### 10.3 隐私与安全

- MVP 数据仅保存在浏览器本地，不上传服务端。
- 描述字段按纯文本展示，避免注入可执行 HTML。
- 界面需明确这是本地单用户体验，不营造已登录或已云同步的误解。

### 10.4 可维护性

- Board、Timeline、任务编辑、国际化、主题和数据访问分离为清晰模块。
- 业务状态使用稳定代码值，显示文本通过翻译资源映射。
- 关键行为至少覆盖自动化测试：任务校验、筛选组合、状态移动、日期范围和偏好持久化。

## 11. 一日交付计划与取舍门槛

以下为单人约 8 小时的建议时间盒：

| 时间 | 工作内容 | 退出条件 |
| --- | --- | --- |
| 0:00–0:45 | 项目初始化、数据模型、主题与国际化骨架 | 页面可运行，语言和主题状态可切换 |
| 0:45–3:15 | Board、卡片、创建/编辑/删除、本地持久化 | 核心任务闭环可用，刷新不丢失 |
| 3:15–4:15 | 拖拽、搜索、筛选 | 跨列移动和组合过滤稳定 |
| 4:15–5:30 | 简化 Timeline | 日期条、未排期、逾期状态正确 |
| 5:30–6:15 | 中英文文案、深浅主题完整覆盖 | 无明显漏翻或不可读区域 |
| 6:15–7:15 | 边界状态、响应式、无障碍基础 | 主验收视口和键盘替代路径可用 |
| 7:15–8:00 | 自动化测试、构建、验收与修复 | P0 验收通过，无阻塞缺陷 |

取舍规则：

1. 任何 P0 主流程不可用时，停止开发 P1。
2. 若拖拽库接入超过 45 分钟仍不稳定，保留跨列拖拽，暂缓列内自由排序，并以最近更新时间排序。
3. 若 Timeline 复杂度超出时间盒，保留只读日期条和详情编辑日期，不实现轴上拖拽与缩放。
4. 若响应式处理超时，确保桌面端验收宽度完整可用，小屏采用横向滚动兜底。
5. 最后 45 分钟锁定功能范围，只修复阻塞问题并完成验证。

## 12. MVP 验收标准

### 12.1 功能验收

- [ ] 首次进入可看到四个状态列和演示任务。
- [ ] 可以创建、查看、编辑和删除任务，必填及日期校验有效。
- [ ] 可以将卡片拖到其他状态列，列计数与任务状态同步更新。
- [ ] 刷新页面后，任务内容、状态和顺序仍然保留。
- [ ] 搜索标题或编号可得到正确结果。
- [ ] 负责人和优先级筛选可单独使用，也可与搜索组合使用。
- [ ] Timeline 能正确展示有日期任务、未排期任务和逾期任务。
- [ ] Board 和 Timeline 打开的是同一个任务，修改后两处数据一致。
- [ ] 中英文切换即时生效，固定文案无明显漏翻，刷新后语言保留。
- [ ] Vercel Light、Vercel Dark 主题卡片均可预览和切换，首次访问默认为 Vercel Dark，刷新后主题选择保留。

### 12.2 质量验收

- [ ] 1280×720 及以上视口没有阻塞操作的遮挡、截断或异常滚动。
- [ ] 最新版 Chrome 中无阻塞级控制台错误。
- [ ] 键盘用户可通过状态下拉框完成拖拽的替代操作。
- [ ] 核心文本在浅色和深色主题下均可读，错误与逾期不只依赖颜色表达。
- [ ] 生产构建成功，核心自动化测试通过。

### 12.3 阻塞级缺陷定义

出现以下任一情况不得视为 MVP 完成：

- 无法创建或保存任务。
- 拖拽后任务丢失、重复或刷新回退。
- 删除错误任务或 Board 与 Timeline 数据不一致。
- 切换语言后关键操作按钮不可理解或切换主题后核心内容不可读。
- 本地数据异常导致应用白屏且无法恢复。

## 13. 交付物

- 可运行的 Web 应用源码。
- 包含演示数据的生产构建。
- 简短 README：启动方式、构建命令、技术说明和已知限制。
- 覆盖核心业务规则的测试。
- 一份按第 12 节执行的验收记录。

## 14. 后续演进路线

### Phase 1：完善单用户项目管理

- Backlog、标签、子任务、自定义列、批量操作。
- Timeline 拖拽排期、周/月缩放、里程碑和任务依赖。
- 导入导出、撤销操作、统计概览和快捷键。

### Phase 2：接入云端与身份

- 将本地数据访问实现替换为后端 API。
- 引入真实登录、用户资料、空间、项目和成员管理。
- 服务端持久化、跨设备同步、审计日志和权限模型。

### Phase 3：多人协作与生态

- 实时协作、评论、@提及、通知和活动流。
- Sprint、估时、报表、自动化规则与模板。
- GitHub/GitLab、日历、消息工具及 Webhook 集成。

### 14.1 为演进预留的实现边界

- `TaskRepository`：隔离本地存储，未来替换为远程 API。
- `IdentityProvider`：MVP 返回固定本地用户，未来接入认证系统。
- `ProjectContext`：MVP 只有一个默认项目，但任务保留可扩展的项目归属边界。
- `Event/Command` 边界：创建、更新、移动、删除通过统一动作执行，便于未来增加撤销、审计和实时同步。
- 国际化资源：使用稳定 key 管理，新增语言无需修改业务逻辑。
- 主题令牌：使用语义化颜色与间距变量，便于扩展品牌主题和组件库。

## 15. 发布决策

满足全部 P0 功能验收、质量验收且不存在阻塞级缺陷时即可发布 MVP。P1 是否完成不影响发布；任何未完成项应记录为已知限制，不以隐藏入口或半成品交互进入发布版本。
