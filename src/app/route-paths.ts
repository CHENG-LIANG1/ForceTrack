/** Central route constants prevent navigation links and route declarations from drifting apart. */
export const routes = {
  summary: '/summary',
  backlog: '/backlog',
  board: '/board',
  timeline: '/timeline',
} as const;
